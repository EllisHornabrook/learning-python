numOne = 3 / 3
    # returns float
numTwo = 3 // 3
    # returns integer
numOne + numTwo
    # returns result of two values, does not change numOne value
numTwo += 20
    # changes original value, also -= /= etc